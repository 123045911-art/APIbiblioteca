services:
  api:
    build: .
    container_name: API_Biblioteca
    ports:
      - "5001:5000"
    volumes:
      - .:/app
    command: uvicorn API.API:app --host 0.0.0.0 --port 5000 --reload --reload-dir /app